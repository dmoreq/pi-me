/**
 * Subagent extension entry — re-exports from the extension implementation.
 * This file exists to give the extension a meaningful name ("subagent")
 * in the extensions list, rather than the generic "extension" directory name.
 */
export { default } from "./extension/index.ts";
